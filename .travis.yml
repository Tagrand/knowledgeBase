os:
  - linux

language: php

php:
  - '7.1'

before_script:
  - composer self-update
  - composer install --no-interaction
  - cp .env.travis .env
  - phpenv rehash
  - nvm install 7.7.1
  - npm install

script:
  - vendor/bin/phpunit --coverage-text
  - npm run production
